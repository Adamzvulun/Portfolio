# Formspree form ID for the contact page.
# 1. Sign up at https://formspree.io (free).
# 2. Create a new form, copy its ID (the part after /f/ in your form URL).
# 3. Paste it below as the value.
# 4. Restart `npm run dev` so the new env var is picked up.
NEXT_PUBLIC_FORMSPREE_ID=
